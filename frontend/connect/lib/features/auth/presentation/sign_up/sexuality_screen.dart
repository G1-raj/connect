import 'package:flutter/material.dart';

class SexualityScreen extends StatelessWidget {
  const SexualityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}